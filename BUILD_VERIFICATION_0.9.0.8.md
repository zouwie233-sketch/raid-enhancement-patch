# BUILD_VERIFICATION_0.9.0.8

## Version

raid_enhancement_patch-0.9.0.8-bossbar-victory-vanilla-final-suppress-alpha

## Required source package checks

1. zip root is Gradle project root: YES, after packaging from repository root contents.
2. build.gradle exists: YES.
3. settings.gradle exists: YES.
4. gradle.properties exists: YES.
5. gradlew / gradlew.bat exist: YES.
6. gradle-wrapper.jar exists: YES.
7. src/main/java exists: YES.
8. src/main/resources exists: YES.
9. neoforge.mods.toml exists: YES.
10. mixin config exists: YES, raid_enhancement_patch.mixins.json.
11. .github/workflows/build-mod.yml exists: YES.

## Build status

Local Gradle clean build: NOT COMPLETED in this sandbox.

Reason:

The sandbox cannot resolve services.gradle.org, so Gradle Wrapper cannot download Gradle 8.12.

Observed command:

./gradlew --version

Observed error summary:

java.net.UnknownHostException: services.gradle.org

## Test JAR creation method

The supplied test JAR was generated by:

1. Using raid_enhancement_patch-0.9.0.7-bossbar-dimension-safe-cleanup-alpha.jar as the runtime baseline.
2. Compiling modified Java classes with javac against the existing mod JAR.
3. Replacing only the relevant compiled classes in the JAR:
   - com/noah/raidenhancement/RaidEnhancementPatch.class
   - com/noah/raidenhancement/raid/RaidIndependentBossbarManager*.class
4. Updating META-INF/neoforge.mods.toml.
5. Updating META-INF/MANIFEST.MF.
6. Repacking the JAR.

This is a test JAR, not a Gradle clean-build artifact. Use GitHub Actions to produce the formal build artifact.

## Generated JAR

raid_enhancement_patch-0.9.0.8-bossbar-victory-vanilla-final-suppress-alpha.jar

SHA-256:

eb1d25d479114584a7dcdc8ecde5655da6d658c8664f18b590b42608212e47e0

## Static verification

- JAR manifest version updated to 0.9.0.8.
- neoforge.mods.toml version updated to 0.9.0.8.
- RaidIndependentBossbarManager contains final-victory-bar-suppress, vanilla-victory-reappeared, final-victory-suppress-summary, and skipped-different-dimension strings.
- ServerBossEventRaidTitleMixin remains not enabled in mixins config.

## Game test status

Not yet tested in Minecraft by the user.

## Known issues to verify

- Whether vanilla no-[REP] victory BossBar still appears after completion.
- Whether completion suppress window ends cleanly with summary logs.
- Whether settlementKeyMode=raidInstance still appears.
- Whether there is no duplicate-blocked-by-history and no @raidInstance:fallback.
