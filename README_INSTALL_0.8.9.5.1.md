# 0.8.9.5.1 安装说明

1. 删除旧的 `raid_enhancement_patch-0.8.9.5-performance-audit-and-cache.jar`。
2. 放入 `raid_enhancement_patch-0.8.9.5.1-world-load-hotfix.jar`。
3. 保留原来的配置文件即可：
   `config/raid_enhancement_patch/village_security.properties`
4. 启动游戏并进入世界测试。

这是 0.8.9.5 的世界加载崩溃热修复版，配置项不需要重新调整。
