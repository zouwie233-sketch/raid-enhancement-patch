# Source Package Status 0.9.1.6

The complete GitHub-ready source project is the formal deliverable.

Included:

- Gradle project and wrapper;
- full Java/resources tree;
- GitHub Actions build workflow;
- install, changelog, build verification, known issues and regression documents;
- full configuration audit CSV and summary;
- test checklist.

Local Gradle clean build is not claimed because the environment cannot resolve `services.gradle.org`.
