# 安装说明：raid_enhancement_patch 0.8.9.8.8

1. 删除 mods 文件夹中的旧版 `raid_enhancement_patch-*.jar`。
2. 只放入 `raid_enhancement_patch-0.8.9.8.8-bossbar-title-mixin-hotfix.jar`。
3. 建议删除一次 `config/raid_enhancement_patch/raid_session_lifecycle.properties`，清掉旧波次运行期状态。
4. 本版不再使用旧自定义 HUD，也不使用底部 actionbar 兜底。
