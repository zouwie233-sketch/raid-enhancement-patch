# 0.9.2.1 源码交付报告

## 交付目标

修复 0.9.2.0 跨存档测试发现的版本级生命周期快照共享问题，同时保持已验证玩法不变。

## 主要变更

- 生命周期与生成队列恢复数据迁入每存档独立的 Minecraft SavedData。
- 持久化网关归属 `RaidRuntimeContext`，停服关闭后不保留服务器强引用。
- 旧全局 sidecar 改为只保留、不读取、不自动迁移。
- 新增负载边界契约和 GitHub Actions 构建门禁。

## 未改变

- 困难度/不祥等级波次规划。
- 原版与补丁袭击者组成。
- 有界生成队列算法和幂等批次。
- 防卡墙、流体、无支撑、越界、未加载区块检查。
- BossBar、结算、奖励、VillageFavor 和 Mixin 集合。

## 交付限制

本地完整 Gradle 编译被 Gradle 分发包网络超时阻断。GitHub Actions 完整构建和 `TEST_CHECKLIST_0.9.2.1.txt` 实机测试仍是最终验收条件。
