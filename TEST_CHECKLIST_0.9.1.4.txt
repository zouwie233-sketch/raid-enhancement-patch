0.9.1.4-victory-settlement-boundary-audit-alpha 测试清单

测试前：
1. 完全关闭游戏。
2. 删除旧的 config/raid_enhancement_patch/key_diagnostics.log。
3. 不删除任何 .properties、settlement history 或 favor 数据。
4. 换入 0.9.1.4 JAR。
5. 完整打赢一场袭击。

A. 基础回归
1. latest.log 是否显示 0.9.1.4-victory-settlement-boundary-audit-alpha。
2. 玩家是否能看到 [REP] BossBar。
3. 第一波击杀后 [REP] 是否下降。
4. 第二波开始时 [REP] 是否一次回满。
5. 第二波击杀后 [REP] 是否继续下降。
6. 袭击完成后 [REP] 是否正常消失。
7. 完成后是否不出现无 [REP] 的原版 victory 条。
8. 是否无本 Mod 崩溃。

B. 既有安全锚点
9. 跨维度 cleanup 是否仍为 skipped-different-dimension。
10. VictoryBarAttachGuard 是否仍记录阻断，且 afterVisible=false、afterPlayers=0。
11. settlementKeyMode=raidInstance 是否正常。
12. 是否无 duplicate-blocked-by-history / duplicate-blocked-by-raid-instance-history 异常重复。
13. 是否无 @raidInstance:fallback。
14. 是否只有一次 accepted-before-rewards。
15. 是否无重复奖励、重复经验或重复 VillageFavor 写入。
16. dimensionDuplicationAny=true 是否仍仅作为审计技术债，keyFormatChange=false。
17. BossBar 审计节流是否仍有效，玩家日志中无 textures/signature 完整载荷。

C. 0.9.1.4 新增边界审计
18. startup 是否出现 victorySettlementBoundaryStage=0.9.1.4-victory-settlement-boundary-audit-alpha。
19. 是否出现 victorySettlementAuditMode=audit-projection-only-no-runtime-consumer-change。
20. 是否出现 raidCompletionResultRuntimeAuthority=false。
21. 是否出现 runtimeSettlementOrderChanged=false。
22. 是否出现 duplicateGuardChanged=false。
23. 是否出现 rewardDispatchChanged=false。
24. 是否出现 villageFavorWriteChanged=false。
25. 是否出现 cleanupChanged=false、keyFormatChanged=false。
26. 是否出现 category=[victory-settlement-boundary]、boundaryPhase=before-history-check。
27. before-history-check 是否显示 completionEligiblePlayersResolved=false。
28. 是否出现 boundaryPhase=accepted-before-rewards。
29. accepted-before-rewards 是否显示 completionEligiblePlayersResolved=true。
30. 是否出现 raidCompletionResultCreated=true。
31. 是否出现 completionRaidInstanceKey、completionVillageKey、completionDimension、completionCenter。
32. 是否出现 completionVictory=true。
33. completionEligiblePlayerCount 是否与实际参与结算玩家数量一致。
34. 是否出现 completionOmenLevel、completionTotalWaves、completionGameTime。
35. accepted 阶段是否显示 completionFieldCompleteness=identity-outcome-participants-omen-waves-time。
36. 是否始终显示 consumersStillUseLegacyArguments=true。
37. 一场袭击正常情况下，victory-settlement-boundary 主要应为 2 条：before-history-check 与 accepted-before-rewards。

测试后上传：
- latest.log
- key_diagnostics.log

快速回报模板：
0.9.1.4 测试结果：
1. latest.log 显示 0.9.1.4：待检查 / 是 / 否
2. [REP] BossBar 可见：是 / 否
3. 第一波击杀后下降：是 / 否
4. 第二波开始一次回满：是 / 否
5. 第二波击杀后继续下降：是 / 否
6. 完成后 [REP] 消失：是 / 否
7. 完成后无原版 victory 条：是 / 否
8. 无崩溃：是 / 否
9. 跨维度 cleanup 正常：待检查 / 是 / 否
10. settlementKeyMode=raidInstance：待检查 / 是 / 否
11. 无 fallback / 重复结算：待检查 / 是 / 否
12. victorySettlementBoundaryStage 出现：待检查 / 是 / 否
13. before-history-check 边界快照出现：待检查 / 是 / 否
14. accepted-before-rewards 边界快照出现：待检查 / 是 / 否
15. accepted 快照玩家数正确：待检查 / 是 / 否
16. consumersStillUseLegacyArguments=true：待检查 / 是 / 否
17. victory-settlement-boundary 日志条数：待检查
