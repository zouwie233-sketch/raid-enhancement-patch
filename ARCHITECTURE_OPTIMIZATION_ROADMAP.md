# Raid Enhancement Patch 架构优化任务进度表

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 架构优化基线 | `0.9.1.12-raid-registration-lifecycle-hotfix` |
| 基线状态 | 困难模式 + 不祥之兆 V、最终连续波次、退出重进和生成队列恢复已通过实机测试 |
| 优化目标 | 在不破坏现有玩法和存档的前提下，逐步消除上帝类、全局静态运行态和分散门控 |
| 构建交付 | 提交可供 GitHub 构建的完整源码 ZIP；不要求交付本地构建 JAR |
| Java 基线 | Zulu JDK 21：`C:\Program Files (x86)\java\zulu21.44.17-ca-jdk21.0.8-win_x64\bin\java.exe` |
| 当前文档状态 | 三项 ADR 已确认；ARCH-0 已完成；ARCH-1.1 已实施并等待 GitHub 构建与实机验证 |

## 2. 状态定义

| 状态 | 含义 |
|---|---|
| 已完成 | 已设计、实现、验证并交付 |
| 待确认 | 存在会影响实现方向的产品或架构决策 |
| 未开始 | 已列入计划，但尚未设计或实现 |
| 设计中 | 正在分析边界、数据所有权和兼容方案 |
| 实施中 | 已确认设计，正在修改代码 |
| 待测试 | 代码已完成，等待自动化或实机验证 |
| 阻塞 | 缺少必要信息、依赖或测试条件 |

## 3. 不可违反的架构约束

1. 架构重构默认不得改变波次数量、袭击者编成、奖励数值、村庄防御规则和原版游戏节奏。
2. 不进行一次性大重写，采用绞杀者模式：先建立新边界，再逐个迁移旧消费者。
3. 每一种事实只能有一个权威所有者；BossBar、日志和结算只能读取投影，不得反向拥有袭击状态。
4. 所有游戏状态由服务端权威维护；客户端只接收展示数据。
5. 不新增无界 tick 扫描、全实体遍历、无预算世界查询或未缓存反射。
6. 新持久化必须属于具体世界、具有格式版本、迁移策略、损坏处理和重复执行保护。
7. 配置必须以不可变快照生效；解析失败不能留下“部分新配置、部分旧配置”的混合状态。
8. Mixin、NeoForge 事件和外部 Mod 兼容代码只能作为适配器，不得直接承载核心玩法规则。
9. 每个阶段都必须保留安全出生点的碰撞、流体、地面、世界边界、区块已加载和 tick 预算检测。
10. 每次交付必须包含变更报告、风险说明、测试清单、源码 ZIP 和 SHA-256。

## 4. 总体阶段进度

| 阶段 | 主题 | 当前状态 | 前置条件 | 主要交付物 |
|---|---|---|---|---|
| ARCH-0 | 基线冻结与架构决策 | 已完成 | 无 | 行为基线、ADR、所有权清单、测试矩阵 |
| ARCH-1 | 服务器作用域与生命周期 | 实施中 | ARCH-0 | `RaidRuntimeContext`、完整停服清理、跨存档隔离 |
| ARCH-2 | 袭击聚合、SSOT 与显式状态机 | 未开始 | ARCH-1 | `RaidEncounter`、`RaidPhase`、统一门控 |
| ARCH-3 | 世界持久化与版本迁移 | 未开始 | ARCH-2 | 版本化 `SavedData`、结算账本、迁移器 |
| ARCH-4 | 拆分上帝控制器 | 未开始 | ARCH-2、ARCH-3 | 小型领域服务、薄兼容外观层 |
| ARCH-5 | 事件管线与故障隔离 | 未开始 | ARCH-2、ARCH-4 | 明确处理阶段、独立预算和错误边界 |
| ARCH-6 | 配置原子化与数据驱动 | 未开始 | ARCH-2 | 不可变配置、Codec、datapack 定义 |
| ARCH-7 | 性能索引与反射隔离 | 未开始 | ARCH-1、ARCH-4 | 实体索引、查询预算、兼容适配器 |
| ARCH-8 | 可观测性与自动化测试 | 未开始 | 可伴随各阶段 | 结构化日志、单元测试、GameTest、压力测试 |
| ARCH-9 | 收尾、删除旧路径和稳定版验收 | 未开始 | ARCH-1 至 ARCH-8 | 删除兼容外观、架构文档、长期维护基线 |

## 5. 详细任务进度表

### ARCH-0：基线冻结与决策

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-0.1 | 审计上帝类、静态状态、SSOT、事件门控、持久化和扫描路径 | 已完成 | 低 | 已形成基于源码证据的架构审计结论 |
| ARCH-0.2 | 固化 `0.9.1.12` 行为基线和现有测试日志 | 已完成 | 低 | 最终波次退出重进、队列恢复、安全生成结果可作为回归基准 |
| ARCH-0.3 | 建立“事实—权威所有者—消费者”矩阵 | 已完成 | 低 | 波次、阶段、队列、村庄防御、结算和展示均只有一个声明的所有者 |
| ARCH-0.4 | 建立架构决策记录 ADR | 已完成 | 中 | ADR-001 至 ADR-003 已于 2026-08-01 确认 |
| ARCH-0.5 | 建立特征测试和回归测试矩阵 | 已完成 | 低 | 已定义 T1-T4、20 个核心用例和 5 个性能守门用例；测试实现尚随各阶段执行 |
| ARCH-0.6 | 建立源码结构依赖规则 | 已完成 | 低 | 已定义 domain/application/infrastructure/integration/presentation/config 依赖方向 |

### ARCH-1：服务器作用域与生命周期

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-1.1 | 引入每个 `MinecraftServer` 独立的 `RaidRuntimeContext` | 待测试 | 中 | 首批已实施；JDK 契约和静态边界验证通过，等待 GitHub 完整构建及跨存档实机测试 |
| ARCH-1.2 | 把 Raid 状态表从进程级静态集合迁入服务器上下文 | 未开始 | 高 | 现有袭击逻辑行为不变，退出主菜单后不残留旧状态 |
| ARCH-1.3 | 迁移村庄防御、BossBar、支援状态和村民保护运行态 | 未开始 | 高 | 所有运行态均声明服务器或世界作用域 |
| ARCH-1.4 | 清理雇佣兵、冷却、回滚任务和诊断限流状态 | 未开始 | 中 | `ServerStoppedEvent` 后无旧世界强引用和旧 `gameTime` 状态 |
| ARCH-1.5 | 增加跨存档和重复开关服隔离测试 | 未开始 | 中 | 同一 JVM 连续进入两个世界不会串状态、串冷却或串 BossBar |

### ARCH-2：袭击聚合、SSOT 与状态机

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-2.1 | 定义类型化 `RaidEncounterId`、`VillageId` 和 `RaidInstanceId` | 未开始 | 中 | 不再用含义不明确的裸字符串在不同领域间传递 |
| ARCH-2.2 | 定义 `RaidPhase` 和合法转换表 | 未开始 | 高 | 非法阶段组合无法构造；每次转换都有原因和时间 |
| ARCH-2.3 | 建立 `RaidEncounter` 聚合根 | 未开始 | 高 | 聚合只拥有状态、不变量和转换，不扫描世界、不生成实体、不写 UI |
| ARCH-2.4 | 建立不可变 `RaidEncounterSnapshot` 投影 | 未开始 | 中 | BossBar、日志、结算只读取同一快照来源 |
| ARCH-2.5 | 正式接入 `RaidVictoryGate` | 未开始 | 高 | Mixin 不再遍历 `ExtraWaveState`，所有胜利判定读取聚合快照 |
| ARCH-2.6 | 统一完成判定并产生一次性 `RaidCompleted` 事件 | 未开始 | 高 | 结算、好感、清理和 BossBar 不再各自推断完成状态 |
| ARCH-2.7 | 消除布尔状态组合，迁移桥接、离开/返回和清场门控 | 未开始 | 高 | 最终连续波次与返回保护行为和基线一致 |

### ARCH-3：世界持久化与迁移

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-3.1 | 将数据分为配置、世界永久数据、可恢复运行态和纯缓存 | 未开始 | 低 | 每个字段都有明确存储类别和生命周期 |
| ARCH-3.2 | 建立版本化 `SavedData` 仓储和 Schema Version | 未开始 | 高 | 数据存于世界目录；未知新版本安全拒绝；旧版本可迁移 |
| ARCH-3.3 | 迁移额外波次生命周期和生成队列 | 未开始 | 高 | 停服时无待生成槽位丢失，不会重复生成已成功实体 |
| ARCH-3.4 | 迁移胜利结算去重账本 | 未开始 | 高 | 不同世界不共享历史；旧历史可保守迁移且不会重复奖励 |
| ARCH-3.5 | 迁移 VillageFavor，移除反射查找世界路径和全局回退依赖 | 未开始 | 中 | 每个世界独立保存；旧 V1/V2 数据有一次性迁移报告 |
| ARCH-3.6 | 按 ADR 处理支援冷却和村庄防御评级恢复 | 未开始 | 中 | ADR 已确认；重启后完整恢复且不可利用重启绕过关键限制 |
| ARCH-3.7 | 增加损坏、断写、旧格式、重复迁移测试 | 未开始 | 高 | 损坏数据不会崩服；迁移可重复执行且结果相同 |

### ARCH-4：拆分上帝控制器

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-4.1 | 从 `RaidExtraWaveController` 抽取 `RaidDiscoveryService` | 未开始 | 中 | 只负责发现和匹配原版 Raid，不修改阶段和 UI |
| ARCH-4.2 | 抽取 `RaidWavePlanner` 与 `RaidCompositionProvider` | 未开始 | 中 | 波次规划为纯逻辑，可不启动 Minecraft 进行单元测试 |
| ARCH-4.3 | 抽取 `RaidSpawnExecutor` | 未开始 | 高 | 只执行队列槽位；继续使用原安全生成最终裁决 |
| ARCH-4.4 | 抽取 `VanillaRaidAdapter` 和 `RaidsEnhancedAdapter` | 未开始 | 高 | 反射、Mixin 细节和外部 Mod API 不泄漏到领域层 |
| ARCH-4.5 | 拆分 `VillageSecurityController` | 未开始 | 高 | 村民索引、傀儡调度、损伤统计、评级和消息分别拥有单一职责 |
| ARCH-4.6 | 拆分 `VictorySettlementController` | 未开始 | 高 | 结算策略、奖励发放、战场清扫和账本持久化相互独立 |
| ARCH-4.7 | 拆分 `RaidIndependentBossbarManager` | 未开始 | 中 | BossBar 仅消费快照，不扫描袭击者、不参与胜利判定 |
| ARCH-4.8 | 拆分 `MercenaryGolemController` | 未开始 | 中 | 召唤、跟随、持久恢复、颜色和伤害策略分离 |
| ARCH-4.9 | 将旧控制器缩减为临时外观并逐步删除 | 未开始 | 中 | 无新代码依赖旧静态大类；外观层不保存状态 |

### ARCH-5：事件管线与故障隔离

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-5.1 | 定义发现、规划、执行、清理、投影五个处理阶段 | 未开始 | 中 | 阶段顺序和依赖被代码表达，不依赖隐含调用顺序 |
| ARCH-5.2 | 为每个模块建立独立错误边界 | 未开始 | 中 | 生成模块异常不会阻止同 tick 的 BossBar 清理或必要结算 |
| ARCH-5.3 | 引入带原因的 `GateDecision` | 未开始 | 中 | 每次拒绝生成、胜利、结算或转换都能提供稳定原因码 |
| ARCH-5.4 | 为关键事件建立幂等键 | 未开始 | 高 | 重放 `WavePlanned`、`RaidCompleted` 不会重复生成或重复发奖 |
| ARCH-5.5 | 统一每级世界的工作预算 | 未开始 | 中 | 多个同时袭击不会各自消耗完整预算造成主线程峰值 |

### ARCH-6：配置和数据驱动

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-6.1 | 将配置分为服务器规则、客户端显示和 datapack 内容 | 未开始 | 低 | 每个配置有明确作用域、默认值、消费者和重载策略 |
| ARCH-6.2 | 建立不可变配置快照和原子切换 | 未开始 | 中 | 任一字段解析失败时完整保留上一份有效配置 |
| ARCH-6.3 | 处理无消费者配置项 | 未开始 | 中 | 每个公开配置必须生效，或通过迁移说明正式废弃 |
| ARCH-6.4 | 将波次编成和特殊敌人池迁移为 Codec 校验的 datapack JSON | 未开始 | 高 | 默认数据复现当前编成；非法实体 ID 和数量在加载时明确报错 |
| ARCH-6.5 | 统一奖励、礼物和交换表的数据加载 | 未开始 | 中 | 使用同一注册表、Codec 和错误报告，不再分别手写解析器 |
| ARCH-6.6 | 支持安全 `/reload` | 未开始 | 高 | ADR 已确认；已开始的袭击固定使用启动时规则快照，新袭击使用重载后的规则 |

### ARCH-7：性能索引和反射隔离

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-7.1 | 移除雇佣兵恢复的周期性 `getAllEntities()` | 未开始 | 高 | 使用实体加载/加入/离开事件维护索引，无全已加载实体遍历 |
| ARCH-7.2 | 为袭击者、受保护村民和特殊实体建立事件驱动索引 | 未开始 | 高 | 常规 tick 读取索引；局部扫描只作为有预算的恢复兜底 |
| ARCH-7.3 | 合并同一袭击内重复 AABB 查询 | 未开始 | 中 | 同一阶段的袭击者/玩家快照可供多个只读消费者复用 |
| ARCH-7.4 | 将反射限制在兼容适配器并统一缓存 | 未开始 | 中 | 领域层不反射；tick 路径不枚举类方法列表 |
| ARCH-7.5 | 建立多人服压力基线 | 未开始 | 中 | 同维度多袭击、多玩家和大量实体场景有 tick 时间与扫描次数记录 |

### ARCH-8：可观测性和测试

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-8.1 | 统一使用结构化 Logger，移除核心路径 `System.out` | 未开始 | 低 | 日志具有事件名、raidId、阶段、原因码和限流键 |
| ARCH-8.2 | 为 Mod 独立日志增加大小、轮转和保留策略 | 未开始 | 中 | 长时间测试不会产生无界单文件；错误信息仍可追踪 |
| ARCH-8.3 | 增加状态机、门控、配置、迁移和幂等性单元测试 | 未开始 | 中 | 核心纯逻辑不依赖启动完整游戏即可验证 |
| ARCH-8.4 | 增加 NeoForge GameTest | 未开始 | 高 | 覆盖最终波次、退出重进、安全生成、同时袭击和结算一次性 |
| ARCH-8.5 | 建立 GitHub 构建门禁 | 未开始 | 中 | 编译、测试、资源校验、Mixin 校验和源码包清单全部通过才允许交付 |
| ARCH-8.6 | 建立诊断等级和隐私/容量边界 | 未开始 | 低 | 默认日志适合长期服务器；详细审计必须显式开启且有自动限流 |

### ARCH-9：收尾和长期维护基线

| ID | 任务 | 状态 | 风险 | 验收标准 |
|---|---|---|---|---|
| ARCH-9.1 | 删除已无消费者的旧状态表、旧门控和旧持久化写入路径 | 未开始 | 高 | 删除前已有迁移版本和至少一个稳定版本的兼容观察期 |
| ARCH-9.2 | 重组包结构并固定依赖方向 | 未开始 | 中 | `domain` 不依赖 `adapter`/`presentation`；模块循环依赖为零 |
| ARCH-9.3 | 将版本演进说明从核心类注释迁入 Changelog/ADR | 未开始 | 低 | 核心类注释只解释当前不变量和设计原因 |
| ARCH-9.4 | 完成长时间多人服务器验收 | 未开始 | 高 | 无跨世界污染、无重复奖励、无漏波、无明显 tick 尖峰和日志失控 |
| ARCH-9.5 | 发布架构稳定基线 | 未开始 | 中 | 完整源码 ZIP、SHA-256、迁移说明、回滚说明和测试报告齐全 |

## 6. 待确认架构决策

| ADR | 问题 | 推荐方案 | 未确认前限制 |
|---|---|---|---|
| ADR-001 | 支援令牌冷却是否跨正常重启保留 | 已确认：保留并存入世界作用域，防止通过重启绕过冷却 | 已解除 |
| ADR-002 | 袭击中途重启后，村庄防御损伤和评级是否恢复 | 已确认：完整恢复，保证最终奖励公平性 | 已解除 |
| ADR-003 | datapack 内容是否支持 `/reload` | 已确认：支持；活动袭击固定使用创建时规则快照 | 已解除 |

## 7. 每个任务的完成定义

一个任务只有同时满足以下条件才可标记为“已完成”：

1. 设计边界和不确定项已经确认。
2. 代码只修改该任务声明的职责范围。
3. 不相关玩法、配置默认值和持久化格式保持不变；如必须变化，提供单独迁移方案。
4. 自动化检查通过，并完成与风险相称的实机测试。
5. 困难模式 + 不祥之兆 V 最终连续波次回归通过。
6. 退出主菜单、重新进入同一存档的恢复测试通过。
7. 没有新增无界实体遍历、同步大量磁盘写入或 tick 高频反射。
8. 输出变更报告、测试报告、已知问题、源码 ZIP 和 SHA-256。

## 8. 推荐的首个实施批次

首批只进行架构基础建设，不改变玩法数值和持久化格式：

1. 完成 ARCH-0.3：权威信息所有权矩阵。
2. 确认 ADR-001 至 ADR-003。
3. 完成 ARCH-0.5：为当前已验证行为建立特征测试矩阵。
4. 设计 ARCH-1.1：`RaidRuntimeContext` 的服务器作用域和关闭协议。
5. 先迁移低风险诊断/缓存状态，再迁移核心袭击状态。

只有首批测试通过后，才进入 `RaidEncounter` 状态机和持久化迁移。
